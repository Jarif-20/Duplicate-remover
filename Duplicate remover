input_string = input("please enter your string: ")
answer = input_string[0]  

for index in range(1, len(input_string)):
    current_character = input_string[index]
    previous_character = input_string[index - 1]
    
    if current_character != previous_character:
        answer += current_character

print(answer)
